// Placeholder content for the SVG icons. Please replace this with the actual SVG data.

const Icon = ({ name }) => {
  switch (name) {
    case 'shuttle':
      return <svg><!-- Complete shuttle SVG here --></svg>;
    // Add other icons as necessary
    default:
      return null;
  }
};

export default Icon;
